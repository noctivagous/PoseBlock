# SAM 3D Body mannequin generation pipeline
#
# Inference weights are loaded from Hugging Face Hub (facebook/sam-3d-body-vith).
# You need gated access + hf auth login. Weights cache under ~/.cache/huggingface.
#
# Python inference code (not weights) comes from a local clone of sam-3d-body:
#   git clone https://github.com/facebookresearch/sam-3d-body
#   export SAM3D_BODY_ROOT=/path/to/sam-3d-body
#   pip install -r tools/sam3d/requirements-sam3d-body-minimal.txt

# Core inference
torch>=2.1.0
torchvision>=0.16.0
huggingface-hub>=0.24.0

# Image processing
opencv-python>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# 3D mesh handling
trimesh>=4.0.0
scipy>=1.11.0

# GLB/glTF direct export (used when Blender is unavailable)
pygltflib>=1.16.0

# Progress + utilities
tqdm>=4.66.0
pyrootutils>=1.0.4
